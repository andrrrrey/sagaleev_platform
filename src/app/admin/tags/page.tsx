import { StagePlaceholder } from '@/components/ui/StagePlaceholder';
export const metadata = { title: 'Админ — теги' };
export default function Page() {
  return <StagePlaceholder kicker="Admin" title="Теги" stage="Этапе 2" />;
}
